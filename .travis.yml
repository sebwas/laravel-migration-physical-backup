language: php
php:
  - '7.0'
  - hhvm
  - nightly

matrix:
  fast_finish: true
  allow_failures:
    - php:
      - hhvm
      - nightly

branches:
  only:
    - master
    - dev

git:
  depth:
    3

install:
  - scripts/install.sh

before_script:
  - phpenv config-rm xdebug.ini
